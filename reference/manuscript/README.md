# Tracked manuscript extracts

These are the exact manuscript source files containing the equations and staged-refinement
requirements cited by the branch tasks. They make every worktree self-contained.

Equation labels in task documents are resolved directly in these TeX files. Treat the manuscript as
physical intent subject to the common conventions and proof hierarchy. Do not edit these extracts in
physics branches. Rebuilding them requires the archived manuscript identified in `MANIFEST.json`.
